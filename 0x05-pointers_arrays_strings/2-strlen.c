#include <stdio.h>
#include <string.h>

/**
 * _strlen - function to get the length of a string
 *
 * @s: pointer to the string
 *
 * Return: length of the string
 */

int _strlen(char *s)
{
	int x = strlen(s);

	return (x);
}

