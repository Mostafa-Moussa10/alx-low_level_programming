#include "main.h"

/**
 * _abs - function to get the absolute value of number
 *
 * @n: the variable  to carry the input
 *
 * Return: the absolute value
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
